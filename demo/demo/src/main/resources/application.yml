spring:
    profiles:
        active: local
    datasource:
        driver-class-name: org.h2.Driver
        url: jdbc:h2:tcp://localhost/~/buildup
#        url: jdbc:h2:~/buildup
        username: sa
        password:
    jpa:
        hibernate:
            ddl-auto: create
        properties:
            hibernate:
                show_sql: true
                format_sql: true